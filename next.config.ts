import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.openai.com", "oaidalleapiprodscus.blob.core.windows.net"],
  }
};

export default nextConfig;
